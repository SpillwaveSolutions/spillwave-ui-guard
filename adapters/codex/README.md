# Codex adapter

Native Codex install uses the repo-root manifests:

- `.codex-plugin/plugin.json`
- `.agents/plugins/marketplace.json`

```bash
codex plugin marketplace add SpillwaveSolutions/spillwave-ui-guard
```

This folder is a pointer only. Do not duplicate skills here — they live in `/skills`.
