#!/usr/bin/env bash
# Soft reminder for agent hosts. Never blocks.
# Pre-commit enforcement lives in hooks/pre-commit-ui-guard.sh.

echo "UI Guard: non-trivial UI work needs a wireframe under wireframes/ and an adversarial review (ui-adversarial-reviewer) before it is done."
exit 0
